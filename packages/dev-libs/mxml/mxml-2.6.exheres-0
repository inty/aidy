# Copyright 2010 Adriaan Leijnse
# Distributed under the terms of the GNU General Public License v2
# Based in part on mini-xml-2.6.ebuild from Gentoo (http://gentoo-portage.com/dev-libs/mini-xml)

require autotools [ supported_autoconf=[ 2.5 ] supported_automake=[ 1.10 ] ]

SUMMARY="Mini-XML is a small XML library"
DESCRIPTION="
Mini-XML is a small XML library that you can use to read and write XML and XML-like data files in your application without requiring large non-standard libraries.
"
HOMEPAGE="http://www.minixml.org/"
DOWNLOADS="http://ftp.easysw.com/pub/${PN}/${PV}/${PNV}.tar.gz"

LICENCES="Mini-XML"
SLOT="0"
PLATFORMS="~amd64"

MYOPTIONS="threads"

BUGS_TO="adriaan@leijnse.net"

DEFAULT_SRC_CONFIGURE_OPTION_ENABLES=( threads )
DEFAULT_SRC_INSTALL_PARAMS=( "DSTROOT=\"${IMAGE}\"" )

